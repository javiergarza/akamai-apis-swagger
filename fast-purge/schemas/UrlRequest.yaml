description: Specifies a list of URLs or ARLs to invalidate or delete. You can submit
  URLs and ARLs in the same purge request.
properties:
  objects:
    items:
      description: Lists URLs or ARLs to purge. Purges content for both `http` and
        `https` schemes.
      type: string
    type: array
required:
- objects
type: object
